apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homer-prod-1-pvc
  namespace: homer-prod-1
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs
  resources:
    requests:
      storage: 500Mi
  volumeName: homer-prod-1-pv
 