apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-prod-2-configmap
  namespace: nginx
data:
  nginx.conf: |
    # Add your nginx configuration here
    server {
      listen 80;
      server_name nginx-prod-2.kube-prod-1.home.clcreative.de;
      location / {
        root /usr/share/nginx/html;
        index index.html;
      }
    }
  default.conf: |
    server {
      listen 80;
      server_name _;
      location / {
        root /usr/share/nginx/html;
        index index.html;
      }
    }
